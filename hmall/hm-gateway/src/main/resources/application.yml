server:
  port: 8080
hm:
  jwt:
    location: classpath:hmall.jks
    alias: hmall
    password: ${JWT_KEYSTORE_PASSWORD:hmall123}  # 默认值仅用于本地开发
    tokenTTL: 30m           # token 总有效期
    refresh-window: 15m     # 续期冷却窗口：距上次签发超过此时间才允许续期
  auth:
    excludePaths:
      - /search/**
      - /users/login
      - /items/**
      - /hi