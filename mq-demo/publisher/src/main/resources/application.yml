logging:
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS
  level:
    com.itheima.publisher: debug
spring:
  rabbitmq:
    host: 192.168.100.128
    port: 5672
    virtual-host: /hmall
    username: hmall
    password: 123
    connection-timeout: 1s
    template:
      retry:
        enabled: true
        multiplier: 2
    publisher-confirm-type: correlated # 异步confirm
    publisher-returns: true
